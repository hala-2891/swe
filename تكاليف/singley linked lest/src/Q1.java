public class Q1 {//بالتأكيد! هنا لك تنفيذ لطريقة equals في سياق فئة SinglyLinkedList. سأشرح الحل خطوة بخطوة.

    //أولاً، دعنا نتأكد من فهمنا لمتطلبات طريقة equals. يجب أن تقوم هذه الطريقة بمقارنة قائمة مرتبطة مع قائمة أخرى للتحقق مما إذا كانت متساوية بنية وقيمة العناصر.

    //في فئة SinglyLinkedList، لدينا رأس (head) تشير إلى العنصر الأول في القائمة المرتبطة. يتم تمثيل العناصر في القائمة المرتبطة عن طريق العقد (Node)، حيث يحتوي كل عقد على قيمة ورابط إلى العقد التالي.

    //هنا هو تنفيذ لطريقة equals في فئة SinglyLinkedList:


    @Override
    public boolean equals(Object obj) {
        if (this == obj) {
            // القوائم المرتبطة نفسها
            return true;
        }

        if (obj == null || getClass() != obj.getClass()) {
            // تحقق من صحة الكائن ونوعه
            return false;
        }

        SinglyLinkedList otherList = (SinglyLinkedList) obj;

        // تحقق من طول القوائم المرتبطة
        if (size() != otherList.size()) {
            return false;
        }

        Node currentNode = head;
        Node otherNode = otherList.head;

        while (currentNode != null) {
            // قارن قيمة العقد الحالي في القائمة المرتبطة الحالية والقائمة المرتبطة الأخرى
            if (!currentNode.value.equals(otherNode.value)) {
                return false;
            }

            currentNode = currentNode.next;
            otherNode = otherNode.next;
        }

        return true;
    }


    //دعونا نشرح بعض النقاط الرئيسية في الحل:

            //1. نستخدم الكلمة المفتاحية @Override للإشارة إلى أننا نقوم بتجاوز طريقة equals في فئة موروثة.

//2. نقوم بفحص إذا ما كان الكائن الذي تم تمريره للطريقة هو نفس الكائن الحالي (this)، في هذه الحالة فإن القوائم المرتبطة هي نفسها وبالتالي تعتبر متساوية.

//3. نقوم بفحص إذا ما كان الكائن الممرر للطريقة فارغًا (null) أو ليس نفس النوع (getClass)، في هذه الحالة فإن القوائم المرتبطة ليست متساوية.

           // 4. نقوم بتحويل الكائن الممرر للطريقة إلى SinglyLinkedList للقيام بالمقارنة.

//5. نقوم بإجراء المقارنة بين القوائم المرتبطة بالاعتماد على قيمة العقد في كل قائمة. إذا كانت القيم غير متساوية، فإن القوائم المرتبطة ليست متساوية.

//6. في النهاية، إذتعتبر القوائم المرتبطة متساوية إذا تمت المقارنة بنجاح لجميع العقد في القوائم المرتبطة ولم يتم العثور على أي اختلاف.

   // أتمنى أن يكون ذلك واضحًا! إذا كان لديك أي أسئلة إضافية، فلا تتردد في طرحها.
}
